export const departmentAnchorId = (slug: string) => `departamento-${slug}`;

export const departmentHeadingId = (slug: string) => `titulo-${slug}`;

export const DEPARTMENT_SCROLL_MARGIN =
  'scroll-mt-[calc(var(--header-height)+var(--categories-rail-height))]';
